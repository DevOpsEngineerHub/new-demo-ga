name: Secret Demo chatGPT 

on:
  push:
  workflow_dispatch:

jobs:
  check-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Show Secret Information
        env:
          DEMO_SECRET: ${{ secrets.DEMO_SECRET }}
        run: |
          echo "Secret value: $DEMO_SECRET"
          echo "Secret length: ${#DEMO_SECRET}"
